/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['mhowwpetxddomyk9.public.blob.vercel-storage.com', 'images.unsplash.com']
    }
};

export default nextConfig;
